# Copy to local.properties (gitignored) and fill in.
# Do NOT commit local.properties.

# Android SDK location — set automatically by Android Studio on first sync.
# Override here if your SDK lives somewhere unusual.
# sdk.dir=/Users/<you>/Library/Android/sdk

# No Rokid build-time secrets are required: phone↔glasses auth is handled
# at runtime by the Hi Rokid AI app via AuthorizationHelper, which returns
# a token the user grants on first launch.
